#!/bin/bash

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
    echo "CFILE variable is not set."
    exit 1
fi

# Run the preprocessor and save the result to 'c' file
gcc -E "$CFILE" -o c

echo "Preprocessing completed. Output saved to 'c' file."

